describe('ColonFilter', function () {
	var parser;
	beforeEach(function () {
		col = new ColonFilter();
	});

	it('should exist', function () {
		should.exist(col);
	});

	describe('getFilterType(token)', function () {

	});

});