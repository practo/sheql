#!/usr/bin/env node

var sh = require('../lib/executor')();
var str = process.argv[2];
var startYear = process.argv[3] || new Date().getFullYear();
var endYear = process.argv[4] || new Date().getFullYear();
var dateCollection = sh.executor(str, new Date(startYear, 0, 1), new Date(endYear, 11, 31));
for (var i = dateCollection.length - 1; i >= 0; i--) {

	console.log(dateCollection[i].toDateString());
}